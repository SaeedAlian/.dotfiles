#!/usr/bin/env bash
set -e

USER_HOME="${USER_HOME:-"/home/$USER"}"
DOTFILES_DIR="${DOTFILES_DIR:-$USER_HOME/.dotfiles}"

. "$DOTFILES_DIR/coreconfig/helpers.sh"

need_bin ffplay

ffplay -window_title webcam -fflags nobuffer -flags low_delay -framerate 30 -fast /dev/video0
